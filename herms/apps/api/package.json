{
  "name": "herms-backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "build": "bun build src/lambda.ts --target=node --outdir=dist",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@herms/db": "workspace:*",
    "@herms/shared": "workspace:*",
    "hono": "^4.13.3",
    "pdf-lib": "^1.17.1",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/bun": "latest",
    "typescript": "^6.0.2"
  }
}
