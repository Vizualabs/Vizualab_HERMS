# Copy this file to .env and replace placeholders locally.
# Never commit .env or paste credentials into source files.
DATABASE_URL=postgresql://runtime_user:password@runtime-host.example/neondb?sslmode=require
MIGRATION_DATABASE_URL=postgresql://migration_user:password@migration-host.example/neondb?sslmode=require
APP_ENV=development
PORT=3001
API_PROXY_TARGET=http://localhost:3001
AUTH_SECRET=generate-a-random-secret-with-at-least-32-characters
SESSION_TTL_SECONDS=28800
SESSION_COOKIE_SECURE=false
BUSINESS_TIMEZONE=Asia/Colombo
BUSINESS_CURRENCY=LKR
# Required by the scheduled escalation Lambda. This is the first escalation occurrence.
PRICE_ESCALATION_EFFECTIVE_DATE=2027-01-01T00:00:00+05:30
# D-03 feature flag: automatic or approval_required.
PRICE_ESCALATION_MODE=automatic
QUOTATION_EXPIRY_DAYS=14
QUOTATION_NUMBER_PREFIX=QT
ORDER_NUMBER_PREFIX=ORD
DELIVERY_NOTE_NUMBER_PREFIX=DN
NOTE_TOKEN_SECRET=generate-a-separate-random-secret-with-at-least-32-characters
NOTE_TOKEN_TTL_SECONDS=259200
PUBLIC_APP_URL=http://localhost:3000
SEED_USER_PASSWORD=set-a-unique-local-password-with-at-least-12-characters
SEED_STORE_NAME=HERMS Main Store
SEED_STORE_ADDRESS=
WHATSAPP_PROVIDER_MODE=mock
# Set these only after choosing a WhatsApp BSP and switching mode to webhook.
# WHATSAPP_PROVIDER_URL=https://provider.example/messages
# WHATSAPP_ACCESS_TOKEN=replace-with-provider-token
# WHATSAPP_SENDER_ID=replace-with-whatsapp-sender-id
# Required only by the scheduled outbox publisher after AWS/SQS is provisioned.
# SQS_NOTIFICATION_QUEUE_URL=https://sqs.region.amazonaws.com/account/herms-notifications.fifo
OUTBOX_BATCH_SIZE=10
OUTBOX_MAX_ATTEMPTS=5
OUTBOX_LEASE_SECONDS=240
