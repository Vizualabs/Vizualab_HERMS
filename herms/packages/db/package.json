{
  "name": "@herms/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "check": "bun run src/check-migrations.ts",
    "migrate": "bun run src/migrate.ts",
    "seed": "bun run src/seed.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@herms/shared": "workspace:*",
    "@neondatabase/serverless": "^1.0.2",
    "drizzle-orm": "^0.44.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.4",
    "typescript": "^6.0.2"
  }
}
